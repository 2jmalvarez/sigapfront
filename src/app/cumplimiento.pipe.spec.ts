import { CumplimientoPipe } from './cumplimiento.pipe';

describe('CumplimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new CumplimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
